# Backend origin only (no trailing slash, no /api — appended automatically).
# Only needed when the frontend and backend are deployed on different
# hosts. Baked into the bundle at build time — set it in the Vercel
# project settings before building.
# VITE_API_BASE_URL=https://your-backend.vercel.app
