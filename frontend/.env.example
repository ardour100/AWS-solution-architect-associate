# Absolute backend base URL (no trailing slash). Only needed when the
# frontend and backend are deployed on different hosts (Vercel + Render).
# Baked into the bundle at build time — set it in the Vercel project
# settings before building.
# VITE_API_BASE_URL=https://your-backend.onrender.com
